import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: '"عييت" — Take a Break App',
  webDir: 'www'
};

export default config;
